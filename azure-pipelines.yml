trigger:
  branches:
    include:
      - azure-pipeline-test-2
    exclude:
      - develop
# variables:
#   - group: FTSKeys


jobs:
- template: build.yml  # Template reference
  parameters:
    job_name: ExampleA_Release
    pool:
      vmImage: 'macOS-10.14'
    variables: 'ExampleAKeys'
    app_name: ExampleA

- template: build.yml  # Template reference
  parameters:
    job_name: ExampleB_Release
    pool:
      vmImage: 'macOS-10.14'
    variables: 'ExampleBKeys'
    app_name: ExampleB

- template: build.yml  # Template reference
  parameters:
    job_name: ExampleC_Release
    pool:
      vmImage: 'macOS-10.14'
    variables: 'ExampleCKeys'
    app_name: ExampleC